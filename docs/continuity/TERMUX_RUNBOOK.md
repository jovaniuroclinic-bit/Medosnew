# Termux runbook

`proot-distro login debian`, `cd /workspace/Medosnew`,
`./scripts/install-termux-launcher`, `exit`; luego `~/medos`. No sobrescribe.
Si los HOME no se comparten imprime copia exacta. **PENDING HUMAN TEST**.
