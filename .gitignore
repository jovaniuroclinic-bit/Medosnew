node_modules
dist
.astro
.wrangler
.dev.vars
.dev.vars.*
!.dev.vars.example
logs
*.log
.env
.env.*
!.env.example

# MEDOS Sentinel runtime data never belongs in Git.
sentinel-offline/node_modules/
sentinel-offline/build/
sentinel-offline/*.db
sentinel-offline/*.db-*
sentinel-offline/models/
sentinel-offline/cases/
sentinel-offline/evidence/
sentinel-offline/backups/
sentinel-offline/logs/
sentinel-offline/config/sentinel.json
sentinel-offline/config/models.json
sentinel-offline/config/privacy.json
sentinel-offline/config/retention.json
*.gguf
*.gpg
*.pem
*.key
*.p12
*.pfx
*.sqlite
*.sqlite3
.n8n/
runtime/
state/
sessions/
credentials/
secrets/
backups/
*.pid
.env.local
.env.production
# Local MEDOS private state
/vault/
/data/
/config/
/runtime/
/state/
/sessions/
/credentials/
/secrets/
/backups/
/tools/
/scripts/anti-phishing

# MEDOS Sentinel local build artifacts
/target/
/target-linux-gnu/
/target-linux-gnu.pre-repair-*/

# Local sensitive material
/config/local/

# MEDOS Sentinel local build artifacts

# Local sensitive material

# MEDOS Sentinel local and sensitive artifacts
